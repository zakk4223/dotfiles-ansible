#!/bin/sh


DOTFILES_BASE="$HOME/proj/dotfiles-ansible"
ANSIBLE_BASE="$DOTFILES_BASE/ansible"
PLAYBOOK_NAME="dotfiles.yml"


#run playbook


git -C "$DOTFILES_BASE" pull
ansible-playbook --ask-become-pass "$ANSIBLE_BASE/$PLAYBOOK_NAME" --vault-password-file ~/.ansible-vault.txt $@
